# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Editor / agent config (machine-local)
.claude/
.vscode/
.idea/
.DS_Store

# Credentials — never commit
*.json
!**/example*.json
# ...but these are metadata, not credentials, and must ship.
!server.json
!.github/**/*.json
.env
.env.*
service-account*.json
credentials*.json
application_default_credentials.json

# Build artifacts
build/
dist/
